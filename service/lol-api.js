var restify = require('restify');

    var client = restify.createJsonClient({
        url: 'https://na.api.pvp.net',
        version: '~1.4'
      });

cliente.get('', {}, function(erro, req, res retorno){

})
